{
  "name": "zengate",
  "version": "0.2.0",
  "description": "Reverse proxy gateway with automatic proxy rotation for opencode.ai/zen free models",
  "type": "module",
  "private": true,
  "main": "gate-docker.ts",
  "scripts": {
    "start": "bun run gate-docker.ts"
  },
  "dependencies": {
    "geoip-lite": "^2.0.3",
    "hpagent": "^1.2.0",
    "socks-proxy-agent": "^8.0.5"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
