{
  "name": "zengate",
  "version": "0.2.0",
  "description": "Reverse proxy gateway with automatic proxy rotation for opencode.ai/zen free models",
  "type": "module",
  "private": true,
  "main": "gate.ts",
  "scripts": {
    "start": "bun run gate.ts"
  },
  "dependencies": {
    "hpagent": "^1.2.0",
    "socks-proxy-agent": "^8.0.5"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
